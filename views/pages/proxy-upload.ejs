<!DOCTYPE html>
<html lang="en">
	<head>
		<%- include('../partials/head'); %>
	</head>
	<body class="container">
		<header><%- include('../partials/header', { user }); %></header>

		<main>
			<div class="row">
				<div class="col-sm-5">
					<div class="jumbotron">
						<h1>Upload via Proxy</h1>
						<p>In this example, you will upload your file to the server which will then be moved over to S3.</p>
					</div>
				</div>

				<div class="col-sm-7">
					<div class="well">
						<form
							method="post"
							enctype="multipart/form-data"
							style="display: flex; flex-direction: column; gap: 0.5rem"
						>
							<div class="mb-3">
								<label for="formFile" class="form-label">Select your file</label>
								<input class="form-control" type="file" name="file" id="formFile" required />
							</div>
							<button type="submit" class="btn btn-success">Upload</button>
						</form>
					</div>
				</div>
			</div>
		</main>

		<footer><%- include('../partials/footer'); %></footer>
	</body>
</html>
